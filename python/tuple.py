# Tuples are collection of element that can accept different data types and can be represented with the () symbol
# Tuples are immutable,heterogeneous,cna't be appended
# Example of a tuple
t = (12, 2, "food", "dog")
# We use indexing to access the elements in a tuple
print(t[0])
